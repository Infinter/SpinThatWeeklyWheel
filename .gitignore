# macOS
.DS_Store
**/.DS_Store

# Logs
*.log

# Env (defensive — app has its own .gitignore too)
.env
.env*.local

# Node (defensive)
**/node_modules/

# Claude Code : settings.json (partagé) est suivi ; la variante locale ne l'est jamais.
.claude/settings.local.json
**/.claude/settings.local.json
