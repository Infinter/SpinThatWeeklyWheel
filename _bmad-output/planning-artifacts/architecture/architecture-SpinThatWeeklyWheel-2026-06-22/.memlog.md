---
scope: Daily Wheel — migration de Spin That Wheel v2.html vers app Next.js (App Router) + Supabase sur Vercel, mono-équipe sans auth
purpose: build-substrate
altitude: feature
updated: 2026-06-22T12:53
---

- (constraint) PRD-settled (inherited, read-only): stack = Next.js App Router + React, déployé Vercel via Git CI. NFR1.
- (constraint) PRD-settled: persistance = Supabase (PostgreSQL) via @supabase/supabase-js, clé anon publique. NFR2/NFR8.
- (constraint) PRD-settled: mono-équipe SANS auth — accès par URL, RLS autorise read/write via clé anon. Risque sécurité acté à documenter. NFR3.
- (constraint) PRD-settled: logique métier (génération EDF) côté client, réutilisée de la page HTML existante. Supabase = couche de persistance. §4 Service Architecture.
- (constraint) PRD-settled: monorepo léger / single app, pas de packages séparés. §4 Repository Structure.
- (constraint) PRD-settled: UI 100% français, responsive ≤520px, charte #0078d4/#eef4fb/cartes/sans dégradés. NFR4/NFR5, §3.
- (constraint) PRD-settled data model proposé (seed): participants, unavailabilities, group_exclusions, holidays, team_off_days, settings (ligne unique). §4 Data Model.
- (reference) Code source à migrer: historique/Spin That Wheel v2.html (lignes 604-1113 = script). Algo EDF lignes 979-1065. Source de vérité pour parité NFR9.
- (decision) [ADOPTED] AD: Cœur EDF = fonction pure generateSchedule(input, rng) dans un module sans dépendance DOM/React/Supabase. Binds: FR11/FR14, Epic 4, tests. Prevents: algo soudé au DOM → intestable, parité NFR9 invérifiable, divergence entre code prod et code testé.
- (decision) [ADOPTED] AD: Aléa injecté — la génération reçoit un rng seedable (ex. mulberry32); prod passe un seed aléatoire, tests un seed fixe. Binds: NFR7, NFR9. Prevents: non-déterminisme empêchant rejouabilité et comparaison de parité avec l'ancienne page.
- (event) Décision n°1 tranchée par l'utilisateur: option A (cœur pur) + rng injecté.
- (decision) [ADOPTED] AD: Deux prédicats canoniques de contrainte. isTeamNonSessionDay(date,settings) = week-end(si option) OU exclusion groupe OU férié OU jour off → aucun Daily (pas un trou). isPersonUnavailable(person,date) = indispo individuelle. isTeamNonSessionDay est la SOURCE UNIQUE, branchée à la fois dans la boucle de génération ET dans le calcul de deadline EDF (getLastConsecAvailDay). Binds: FR7/FR8/FR11, Epic 3+4. Prevents: contrainte ajoutée à la boucle mais oubliée dans le calcul de deadline → fenêtre EDF fausse, planning faux et rupture de parité silencieuse.
- (decision) AD: tables holidays et team_off_days restent séparées (distinction métier PRD §4) mais l'algo ne voit que le prédicat unifié — le mapping data→prédicat est la seule couture.
- (event) Décision n°2 tranchée: modèle à deux prédicats, isTeamNonSessionDay source unique.
- (decision) [ADOPTED] AD: Supabase = source canonique de l'état. Le store client est une copie de travail hydratée au chargement; aucune décision métier sur données non confirmées (hors affichage optimiste réconcilié). Binds: FR13, all writes. Prevents: deux clients divergeant sur des copies locales jamais alignées sur la base.
- (decision) [ADOPTED] AD: Écritures OPTIMISTES — update store client immédiat, write Supabase en arrière-plan, rollback + message d'erreur si l'écriture échoue. Binds: édition inline §3, FR1-FR10. Prevents: latence réseau visible à chaque clic; état affiché incohérent en cas d'échec d'écriture non géré.
- (decision) [ADOPTED] AD: Fraîcheur inter-clients = abonnement Supabase Realtime en v1 (choix utilisateur, surclasse le refetch). RÈGLE de réconciliation: l'événement Realtime de sa PROPRE écriture ne doit pas être ré-appliqué (dédup par id/version) pour éviter double-application/flicker. Binds: FR13. Prevents: écho Realtime des mutations locales causant clignotement ou écrasement d'un état optimiste plus récent.
- (event) Décision n°3 tranchée: optimiste (A) + Realtime (surclasse mon lean refetch). Réconciliation des échos requise.
- (decision) [ADOPTED] PARADIGME: architecture en couches. UI(composants) → State(store+hooks) → Data-access(repository) → Supabase. Domaine pur (lib/domain) = feuille, ne dépend de rien.
- (decision) [ADOPTED] AD: lib/data/ (repository) est le SEUL module autorisé à importer/appeler le client Supabase. Aucun composant ni hook ne fait supabase.from(...) en direct. Binds: NFR2, all data ops. Prevents: requêtes dispersées dans les composants, duplication du mapping table↔code.
- (decision) [ADOPTED] AD: Règle de dépendance — les dépendances descendent UI→state→data-access, jamais ne remontent; le domaine pur (lib/domain) ne dépend de personne et personne ne le force à dépendre vers l'extérieur. Binds: AD cœur pur. Prevents: cycle qui re-couplerait l'algo à React/Supabase et le rendrait intestable.
- (direction) Arborescence-graine validée: app/ (routes), components/ (UI), lib/domain (pur), lib/data (repository Supabase), lib/supabase (init client), lib/store (état+Realtime), supabase/migrations (SQL+RLS).
- (event) Décision n°4 tranchée: paradigme en couches + règle d'or 'seul lib/data touche Supabase' + arborescence validée.
- (decision) [ADOPTED] AD: Posture de sécurité B2 — passphrase d'équipe partagée qui verrouille RÉELLEMENT les écritures. Binds: NFR3/NFR8, all writes. Prevents: accès en écriture par quiconque connaît l'URL (garde UI seule = décor contournable via endpoint anon).
- (decision) [ADOPTED] AD: Chemins de données ASYMÉTRIQUES (révise l'AD paradigme). LECTURES: client lib/data → Supabase via clé anon (client-direct). ÉCRITURES: client lib/data → POST vers Route Handler Next.js (app/api/) qui valide la passphrase puis écrit via service_role. Binds: NFR2/NFR3/NFR8, all data ops. Prevents: écritures non gardées; service_role côté client.
- (decision) [ADOPTED] AD: RLS — policies SELECT ouvertes à anon sur les 6 tables applicatives uniquement; INSERT/UPDATE/DELETE NON accordés à anon (refusés). Seules les Route Handlers serveur écrivent (service_role contourne RLS). Binds: NFR3. Prevents: écriture directe via clé anon contournant la passphrase.
- (decision) [ADOPTED] AD: Secrets — service_role et passphrase = variables d'env SERVEUR uniquement (Vercel), jamais NEXT_PUBLIC. Seules NEXT_PUBLIC_SUPABASE_URL et NEXT_PUBLIC_SUPABASE_ANON_KEY sont exposées au client. Binds: NFR8. Prevents: fuite de service_role/passphrase dans le bundle client.
- (decision) AD: lib/supabase expose DEUX clients — client anon (browser, lectures+Realtime) et client admin service_role (server-only, importé uniquement par app/api/). Realtime fonctionne côté lecture via anon+RLS SELECT, inchangé.
- (direction) Arborescence révisée: ajout app/api/ (Route Handlers d'écriture, server-only). lib/data sépare reads (anon) et writes (fetch vers /api).
- (constraint) DIVERGENCE PRD à reporter au finalize: B2 contredit §4 'pas d'API routes obligatoires en v1' (introduit un proxy d'écriture) et l'AD initial 'client écrit direct dans Supabase'. Offrir mise à jour du PRD §4 + NFR3.
- (event) Décision n°5 tranchée: sécurité B2 (passphrase + write-proxy serveur). AD paradigme révisé en chemins asymétriques.
- (decision) [ADOPTED] AD: Parité NFR9 mécanique — test golden rejouant un même jeu de données + même seed dans generateSchedule, comparé à un fixture de référence (ancienne page). NFR9 satisfait ⟺ le test de parité passe (pas de vérif à l'œil). Binds: NFR9, Story 4.2 AC6. Prevents: régression de parité non détectée.
- (decision) [ADOPTED] Convention: dates métier = chaînes YYYY-MM-DD manipulées en local (jamais UTC/toISOString). Binds: tout traitement de date, FR11. Prevents: décalage d'un jour selon le fuseau horaire.
- (decision) [ADOPTED] Convention: table settings = id fixe constant + upsert (jamais insert multiple). Binds: FR13 settings. Prevents: lignes de config multiples / ambiguës.
- (decision) [ADOPTED] AD: Un seul projet Supabase; variables d'env par environnement Vercel (Production=Preview, même base); .env.local en dev pointe sur le même projet. Migrations SQL versionnées dans supabase/migrations. Binds: NFR1/NFR2, déploiement. Prevents: dérive de config entre environnements.
- (direction) Deferred: import one-shot des données localStorage de l'ancienne page. Condition de reprise: si la ressaisie manuelle est jugée trop coûteuse. (PRD le dit optionnel.)
- (event) Décision n°6 tranchée: parité golden + convention dates YMD-local + settings upsert + un seul projet Supabase + migration localStorage différée.
- (version) Next.js 16.2.x (16.2.9 LTS au 2026-06) — App Router stable, Turbopack par défaut, Node 20+ requis. Vérifié web.
- (version) React 19 (fourni avec Next 16). Vérifié web.
- (version) @supabase/supabase-js 2.108.x (ligne 2.x, 2026-06). Realtime + RLS opérationnels. Vérifié web.
- (version) TypeScript (dernière stable via create-next-app). Node 20+ (imposé par Next 16).
- (decision) [ADOPTED] Starter greenfield: create-next-app (App Router, TypeScript, ESLint) → pré-décide arborescence + config Next 16. Puis ajout @supabase/supabase-js. Binds: NFR1. Prevents: config Next hand-rollée incohérente.
- (event) Versions vérifiées sur le web (Next 16.2.x, React 19, supabase-js 2.108.x). Route Handlers Next 16 valident le write-proxy B2.
- (decision) [ADOPTED] AD-14 (write endpoint contract): UNE Route Handler par table (app/api/{table}/route.ts). Enveloppe unique {op:'insert'|'update'|'delete', id?, data?}, passphrase dans header x-team-passphrase. update = patch partiel. Le serveur applique une allowlist de colonnes par table. Binds: AD-7, all writes. Prevents: deux callers envoyant des protocoles d'écriture incompatibles (partiel vs full-row, endpoints divergents).
- (decision) [ADOPTED] AD-15 (ids + versioning, durcit AD-6): le SERVEUR possède les ids (uuid Postgres default). Toutes les tables écrivables portent updated_at (timestamptz, maj serveur). Dédup Realtime = match id ET match updated_at; sinon appliquer. Binds: FR13, AD-5/6. Prevents: echo INSERT avec id non vu (double-render) + dédup-par-id supprimant à tort l'UPDATE d'un AUTRE client (divergence permanente).
- (decision) [ADOPTED] AD-16 (conflits same-row): Last-Write-Wins par ligne, ordonné par updated_at serveur; l'écho/refetch fait autorité sur la réponse de l'écriture optimiste. Binds: AD-4/5/6. Prevents: lost update invisible quand deux clients éditent la même ligne (le dédup cache la régression).
- (decision) [ADOPTED] AD-17 (taxonomie d'erreurs d'écriture): la Route Handler renvoie des classes typées — 401 auth (re-prompt passphrase, pas de retry), 400 validation (rollback), 409 conflit, 5xx transitoire (retry possible). Le client mappe vers un WriteError typé. Binds: AD-5/8. Prevents: client incapable de distinguer auth/validation/transitoire → retry d'une mauvaise passphrase ou perte d'une edit rejouable.
- (decision) [ADOPTED] AD-6 durci (Realtime activé): la migration ajoute les 6 tables à la publication supabase_realtime + REPLICA IDENTITY FULL; Story 1.2 doit prouver qu'un événement arrive. + Re-hydrater le store à chaque (re)connexion Realtime (les connexions publiques se coupent ~24h). Prevents: FR13 mort silencieusement (aucun événement livré) sans aucun AD violé.
- (decision) [ADOPTED] AD-3 durci: signature pinnée isTeamNonSessionDay(date: string /*YMD*/, ctx: {skipWeekends, groupExclusions, holidays, teamOffDays}): boolean — reçoit les contraintes en ARGUMENTS (pur, ne lit aucun store). + test paramétré prouvant que la boucle ET getLastConsecAvailDay appellent le MÊME prédicat. Prevents: deux implémentations divergentes du prédicat.
- (decision) [ADOPTED] AD-2 durci: interface rng = () => number renvoyant [0,1) (compatible Math.random), pour que le shuffle soit substituable.
- (constraint) Convention durcie: settings.id = constante littérale fixe 'singleton'. ON DELETE CASCADE sur unavailabilities.participant_id (FR4) déclaré au niveau DB. Wire-format des dates = chaînes YYYY-MM-DD (DB type date).
- (decision) [ADOPTED] AD-12 reformulé (périmètre parité): le test golden couvre les contraintes LEGACY (week-end, exclusions groupe, indispos) uniquement. L'interaction fériés/off avec la deadline EDF est une EXTENSION comportementale avec ses propres tests dédiés, PAS de la parité. Prevents: confondre extension et parité, laissant l'extension non testée.
- (decision) [ADOPTED] Enveloppe opérationnelle: migrations appliquées via Supabase CLI (supabase db push) ou SQL dashboard; CI exécute les tests domaine (Vitest); le déploiement Vercel est conditionné aux tests verts. Binds: NFR1, Epic 1. Prevents: dimension opérationnelle (CI/application des migrations) laissée silencieuse.
- (decision) AD: section 'Divergences assumées vs PRD' explicitée dans la spine — B2/write-proxy surclasse NFR3 (§4 'pas d'API routes'); à reporter au PRD.
- (version) Correction reviewer: clés Supabase legacy anon/service_role en cours de dépréciation (projets post-2025-11 utilisent sb_publishable_ / sb_secret_). AD-10 reformulé key-format-agnostic: clé LOW-PRIVILEGE (publishable/anon) côté client, clé SECRET (secret/service_role) serveur uniquement. Modèle RLS inchangé.
- (version) Corrections: React 19.2 (paire avec Next 16), Node 20.9+, TypeScript 5.1+. Test runner = Vitest. create-next-app: pin flags --app --ts --eslint, SANS Tailwind (on ratifie la charte CSS existante), sans src/.
- (event) Reviewer Gate: lint 0; rubrique PASS-WITH-FIXES; versions OK+1HIGH; adversariale 10 paires. Tous les fixes clairs appliqués (AD-14..AD-17, durcissements AD-2/3/6/12, corrections versions).
- (event) spine finalized — status final, 17 ADs, lint 0, reviewer gate appliqué
- (event) Divergences reportées dans docs/prd.md v1.1: NFR3 (passphrase+proxy), §4 Service Architecture (write-proxy, domaine pur), §4 Data Model (updated_at, settings.id), §4 Auth (B2), Story 1.2 AC2 (RLS write refusé).
